{
  "name": "kiosk-common",
  "version": "1.0.0",
  "description": "shared kiosk code",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "postbuild": "npm pack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mdiplacido",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^12.0.8",
    "typescript": "^3.5.2"
  },
  "dependencies": {
    "chalk": "^2.4.2",
    "log4js": "^4.3.1"
  }
}
